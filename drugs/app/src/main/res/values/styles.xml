<resources>

    <!-- Base application theme. -->
    <style name="AppTheme_old" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_back</item>
        <item name="android:listDivider">@drawable/bg_divider</item>
    </style>
    <style name="PhotoPickerTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="actionBarTheme">@style/actionBarTheme</item>-->
        <!--<item name="actionBarPopupTheme">@style/actionBarPopupTheme</item>-->
    </style>

    <style name="mypopwindow_anim_style">
        <item name="android:windowEnterAnimation">@animator/push_bottom_in</item>
        <!-- 指定显示的动画xml -->

        <item name="android:windowExitAnimation">@animator/push_bottom_out</item>
        <!-- 指定消失的动画xml -->
    </style>

    <style name="popwindow_up_anim_style">
        <item name="android:windowExitAnimation">@animator/top_push_out</item>
        <!-- 指定显示的动画xml -->

        <item name="android:windowEnterAnimation">@animator/top_push_in</item>
        <!-- 指定消失的动画xml -->
    </style>

    <style name="toolbar_height">
        <item name="android:layout_height">46dp</item>
        <item name="android:minHeight">46dp</item>
    </style>

</resources>
